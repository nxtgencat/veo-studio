# Local / compose env. Git-ignored (.env*). Accessed as password in the UI.
VEO_PASSWORD=change-me-to-a-long-random-string

# Optional backend auth (only if a project uses env auth mode):
GOOGLE_CLOUD_PROJECT=
VERTEX_ACCESS_TOKEN=
VERTEXAI_LOCATION=us-central1
LOG_LEVEL=info
